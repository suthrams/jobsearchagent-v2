{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_doc": "Project-wide Claude Code settings for jobsearchagent-v2. Tracked in git so the secret-audit rule travels to every clone. Personal overrides live in .claude/settings.local.json (gitignored).",

  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash tools/git-hooks/claude-code-bash-audit.sh",
            "timeout": 30,
            "statusMessage": "Auditing git change for secrets..."
          }
        ]
      }
    ]
  }
}
