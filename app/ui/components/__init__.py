"""Shared Streamlit render components for the v2 UI.

Phase 0 of the UI refactor (docs/architecture/ui_refactor_plan.md). Reusable
render helpers (bullets, tailoring cards, track tables, status badges) move here
from streamlit_app.py in later phases. Empty for now - this package exists so the
dispatch registry and the structure tests have a stable home to import.
"""
