# ─── AI ──────────────────────────────────────────────────────────────────────
# Official Anthropic SDK — the only way we call Claude
anthropic>=0.40.0

# ─── Data validation ─────────────────────────────────────────────────────────
# Pydantic v2 — enforces data shapes at every agent boundary
pydantic>=2.0.0

# ─── Configuration ───────────────────────────────────────────────────────────
# Reads config.yaml into Python objects
pyyaml>=6.0
# Loads .env file so ANTHROPIC_API_KEY is available as an environment variable
python-dotenv>=1.0.0

# ─── HTTP and scraping ───────────────────────────────────────────────────────
# Async-capable HTTP client — used by scrapers to fetch job pages
httpx>=0.27.0
# Parses Indeed and Glassdoor RSS feeds without needing a browser
feedparser>=6.0.0
# Extracts text from your resume PDF
pdfplumber>=0.11.0
# Parses HTML from scraped job pages to extract description text
beautifulsoup4>=4.12.0

# ─── Reliability ─────────────────────────────────────────────────────────────
# Retry logic with exponential backoff for API calls and scrapers
tenacity>=8.0.0

# ─── Terminal output ─────────────────────────────────────────────────────────
# Beautiful terminal formatting for run summaries and progress display
rich>=13.0.0

# ─── Dashboard ───────────────────────────────────────────────────────────────
# Browser-based results dashboard — run with: streamlit run dashboard.py
streamlit>=1.35.0
pandas>=2.0.0
plotly>=5.20.0

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest>=8.0.0
